#ifndef FORWARD_DECLARATIONS_H
#define FORWARD_DECLARATIONS_H

namespace SpatialAlgebra {
  template <typename T>
  struct cscalar;

  template <typename T>
  struct cscalar_expr;

  template <typename T>
  struct cmatrix;

  template <typename T>
  struct cmatrix_expr;
}

#endif
